server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gawlasmac/microservices-properties
          default-label: master
management:
  endpoints:
    web:
      exposure:
        include: refresh